name: data
description: Durable message, contact and outbox storage.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  core_protocol: ^0.1.0
  sqlite3: ^2.4.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
