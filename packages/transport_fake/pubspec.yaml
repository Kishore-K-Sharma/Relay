name: transport_fake
description: In-memory N-node mesh simulator. Test-only. Makes mesh behaviour testable without hardware.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  core_protocol: ^0.1.0
  transport_api: ^0.1.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
