name: transport_ble
description: The Bluetooth LE mesh transport. Owns the platform channel to the native radio layer and nothing else.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  transport_api: ^0.1.0
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  pigeon: ^26.0.4
