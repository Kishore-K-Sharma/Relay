name: core_crypto
description: Noise XX handshake, AEAD sessions and key derivation. Pure Dart, no storage, no networking.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  cryptography: ^2.7.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
